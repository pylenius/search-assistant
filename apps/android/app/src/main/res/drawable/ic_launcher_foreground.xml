<?xml version="1.0" encoding="utf-8"?>
<!-- Adaptive-icon foreground: a stylized magnifying glass centered in
     a 108×108 dp viewport. Geometry matches the iOS launcher icon.

     Everything stays inside the 66dp centre circle that every launcher
     mask is guaranteed to show. The first version reused the iOS scale
     directly, which put the handle tip at (80,88) — outside the mask, so
     round launchers sliced the end off the handle. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">

    <!-- Lens ring -->
    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="6"
        android:fillColor="#00000000"
        android:pathData="M 50.25 50.25 m -16.5 0 a 16.5 16.5 0 1 0 33 0 a 16.5 16.5 0 1 0 -33 0" />

    <!-- Handle -->
    <path
        android:strokeColor="#FFFFFFFF"
        android:strokeWidth="6"
        android:strokeLineCap="round"
        android:fillColor="#00000000"
        android:pathData="M 62.25 62.25 L 74.25 74.25" />

    <!-- Center dot -->
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M 50.25 50.25 m -3 0 a 3 3 0 1 0 6 0 a 3 3 0 1 0 -6 0" />
</vector>
