<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- No ACCESS_BACKGROUND_LOCATION on purpose. Recording keeps running
         with the screen off via the location-typed foreground service
         below, which only needs while-in-use permission because the
         service is always started from a visible screen. Declaring the
         background permission would force Google Play's background-
         location review (video demo + justification) for a capability the
         app never actually uses — nothing requests it at runtime. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- zxing-android-embedded ships a scanner Activity and declares CAMERA
         plus camera hardware features. ShareSheet only uses BarcodeEncoder
         to *draw* a QR code, so none of that is needed — and an unexplained
         camera permission on a location app is exactly the kind of thing
         that draws Play review questions. -->
    <uses-permission android:name="android.permission.CAMERA" tools:node="remove" />
    <uses-feature android:name="android.hardware.camera" tools:node="remove" />
    <uses-feature android:name="android.hardware.camera.autofocus" tools:node="remove" />
    <uses-feature android:name="android.hardware.camera.flash" tools:node="remove" />
    <uses-feature android:name="android.hardware.camera.front" tools:node="remove" />

    <application
        android:name=".SearchAssistantApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.SearchAssistant">

        <!-- Maps API key — injected from local.properties by the
             secrets-gradle-plugin. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="${MAPS_API_KEY}" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.SearchAssistant">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links — verified once /.well-known/assetlinks.json
                 is served with the matching package + SHA-256. Until
                 then this still works as a regular http link handler. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="searchassistant.eport.fi"
                      android:pathPrefix="/s/" />
            </intent-filter>
        </activity>

        <!-- Foreground service for path recording + position sharing.
             foregroundServiceType=location is required on API 34+ when
             requesting locations while the screen is off. -->
        <service
            android:name=".location.LocationService"
            android:exported="false"
            android:foregroundServiceType="location" />

    </application>

</manifest>
